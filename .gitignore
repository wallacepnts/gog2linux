# packaged games: hundreds of MB each, rebuildable from the installers
*.pc/
*.wtgz
*.wsquashfs

# local wine prefix
.prefix/

# installer inbox: whatever is dropped in install/ is somebody's download, and
# GOG's multi-GB setup files must never end up in a commit
/install/*
!/install/.gitkeep
setup_*.exe
*.bin
