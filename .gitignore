# packaged games: hundreds of MB each, rebuildable from the installers
*.pc/
*.wtgz
*.wsquashfs

# local wine prefix
.prefix/

# installer staging: <game>/ in the repo root becomes <game>.pc, and GOG's
# multi-GB setup files must never end up in a commit
setup_*.exe
*.bin
